# 架构升级说明：Dify 单LLM → 多阶段工程化流水线

## 对比总览

| 维度 | 原 Dify 工作流 | 当前 v1.2 |
|------|---------------|----------|
| LLM 调用次数 | 1 次 | 2 次（+1 层纯代码校验） |
| 单 LLM 职责 | 识别+判断+输出全包 | Extract（要素）→ Judge（状态），职责分离 |
| 输出 schema | `amount_raw/normalized`、`price_raw/price`、`term_raw/term` 双字段 | 单字段 + `id` + `direction` + `evidence` + `field_sources` |
| 交易标识 | account 作唯一 key（account 为空时尴尬） | T1/T2 内部 ID，account 可以为空 |
| 状态追踪 | 无跨消息状态（每次独立调用） | StateStore 跨消息维护 ConversationState |
| 错误恢复 | LLM 出错整条丢 | 超时/连接错误自动重试 3 次（指数退避） |
| 输出可审计性 | 只有最终 JSON | 每步中间输出 + StateChange 变更记录 |
| 提示词 | 204 行规则 + 5 个强制示例 | Extract 167 行 + Judge 109 行，各含 8 个对照示例 |
| 运行依赖 | Dify 平台 | 纯 Python CLI，`python run_repo_recognizer.py` |

---

## 一、架构演进

### 原 Dify 模式

```
Excel 输入 → [黑盒] → 单次 LLM 调用 → JSON 输出
                  ↑
           提示词.md (204行)
           一次调用完成:
           - 要素提取（amount/term/price/account）
           - 意图分类（询价/报价/成交/拒绝/...）
           - 状态判定（negotiating/confirmed/rejected）
```

**问题**：
- 单 LLM 同时处理三件事（识别+判断+状态），prompt 长但职责混乱
- 没有跨消息状态——每条消息独立处理，不知道"上一轮发生了什么"
- account 作唯一 key，口头交易（account 为空）无法追踪
- 无重试、无可审计中间输出

### 当前 v1.2 架构

```
Excel 输入
  │
  ▼
load_messages()           ← 纯代码，读 Excel
  │
  ▼ (逐条处理)
┌────────────────────────────────────────────┐
│  engine.process(message)                  │
│                                           │
│  ① Extract LLM    要素识别                │
│     ├─ 输入: current_state + history      │
│     │        + known_trade_index          │
│     └─ 输出: trades + changes             │
│              + linking_reason             │
│              + status_signals             │
│              + ambiguity                  │
│                                           │
│  ② normalize IDs   T1/T2 稳定化           │
│     └─ 纯代码，不做业务判断               │
│                                           │
│  ③ Judge LLM      状态判定                │
│     ├─ 输入: 完整 normalized_extract      │
│     │        + linking_reason/signals     │
│     └─ 输出: status + intent + confidence │
│                                           │
│  ④ Verifier       矛盾校验（纯代码）       │
│     ├─ Judge 漏判 trade？                 │
│     ├─ Judge 用了不存在的 ID？            │
│     ├─ confirmed 无 reason？              │
│     ├─ price 疑似 95z/债券代码？           │
│     └─ 同 account 未复用 ID？             │
│                                           │
│  ⑤ StateStore     机械合并               │
│     ├─ 按 ID 覆盖字段                     │
│     └─ 记录 StateChange（before→after）   │
│                                           │
│  ⑥ finalize        消息归档              │
└────────────────────────────────────────────┘
  │
  ▼
write_results()           ← 输出 Excel（原始列 + 9 个新增列）
```

---

## 二、关键改进详解

### 1. 单 LLM → 双 LLM 职责分离

| | Extract LLM | Judge LLM |
|---|---|---|
| 做什么 | 识别交易要素 | 判定交易状态 |
| 不做什么 | 不判 confirmed/rejected | 不改 amount/price/term |
| 输出 | account/amount/term/price/direction/evidence/field_sources/changes | status/intent/confidence/reason |
| 提示词长度 | 167 行 | 109 行 |

**为什么更好**：原版一个 LLM 既要识别 41 是 1.41 利率，又要判断"好滴"是不是成交。职责混在一起，prompt 越长越容易漏。拆开后每个 LLM 的 prompt 更短、更聚焦、更不容易出错。

### 2. 跨消息状态追踪（StateStore）

原 Dify 工作流每次调用无状态。当前系统按 `con_ID` 维护 `ConversationState`：

```
con_ID=6:
  messages: [msg1, msg2, msg3, msg4, msg5, msg6]  ← 完整历史
  trades:
    T1: {account: "圆融安享10号", amount: "5550万", term: "隔夜", status: "confirmed"}
    T2: {account: "锦鸿1号", amount: "15410万", term: "隔夜", status: "confirmed"}
    T3: {account: "锦鸿3号", amount: "7820万", term: "隔夜", status: "confirmed"}
  archived_trades: []
```

每条新消息都在前一条的基础上更新，LLM 看到的是完整上下文。

### 3. T1/T2 内部 ID 替代 account 作 key

原版用 account 字符串作唯一标识——account 为空时无法追踪。现在用内部 ID：

```
口头阶段（account 未知）:
  T1: {account: "", amount: "1亿", term: "14D", direction: "正回购"}

后续发明细:
  T1: {account: "锦鸿2号", amount: "1亿", term: "14D"}  ← 复用 T1，补充 account
```

`known_trade_index` 帮 LLM 记住已有 ID，`normalize_extract_ids` 保证 ID 稳定。

### 4. Extract → Judge 结构化信号

Extract LLM 不只输出 trades，还输出三个信号字段：

- **linking_reason**："当前消息为什么对应 T2" — 帮 Judge 理解对齐逻辑
- **status_signals**："观察到隐式成交词'都发汇享'" — 观察但不裁决
- **ambiguity**："无法确定金额是总量还是每期限的量" — 标注不确定

Judge 收到完整 `normalized_extract_result`（不只是 trades 列表），可以看到 Extract 的判断依据。

### 5. Verifier 校验层

纯代码，不调 LLM。在 Judge 之后、StateStore 合并之前运行，检查 6 类矛盾：

| 检查 | 说明 |
|------|------|
| missing_verdict | Judge 漏判了某个 trade |
| unknown_id | Judge 用了 Extract 没输出的 ID |
| archived_id | 引用了已归档的交易 |
| confirmed_no_reason | confirmed 但理由过短 |
| suspicious_price | price 疑似 95z/债券代码/评级 |
| duplicate_account | 同 account 未复用已有 ID |

### 6. 自动重试

超时/连接断开/HTTP 5xx → 最多重试 3 次，指数退避（1s/2s/4s）。HTTP 4xx / JSON 解析错不重试（重试无意义）。

### 7. 可审计输出

输出 Excel 从原来的少量列扩展到 **9 个新增列**：

| 列 | 内容 |
|----|------|
| 识别LLM输出json | Extract LLM 原始输出 |
| 识别LLM稳定ID输出json | ID 稳定化后的版本 |
| 判断LLM输出json | Judge LLM 输出 |
| 校验器输出json | Verifier 发现的矛盾 |
| llm_used | Y/N |
| llm_error | 错误信息 |
| 状态变更摘要json | 每条字段变化 before→after+来源行号 |
| 最终状态json | 当前 ConversationState 完整快照 |
| 预期格式输出 | 简化版 account/amount/term/price/status/intent |

---

## 三、提示词对比

| | 原 Dify `提示词.md` | 当前 Extract | 当前 Judge |
|---|---|---|---|
| 行数 | 204 | 167 | 109 |
| 示例数 | 5（全正例） | 8（正例+反例） | 8（正例+反例+错判警示） |
| 金额字段 | raw + normalized 双字段 | 单字段 | 不涉及 |
| 状态判断 | 10 条优先级规则 | 不判断 | 上下文推断+隐式成交 |
| 特色规则 | IS_START 强制创建 | 历史继承（报价锚定/议价不覆盖/同类追加） | 价格未达改判/部分确认 |

---

## 四、与实际测试的对应

用 `交易下文_测试集.xlsx`（38 行，6 个 con_ID）跑 v1.2：

- **LLM 调用成功率**：38/38（100%）
- **Verifier 告警**：3 条（Judge 输出的 ID 与 Extract 不完全对齐，Verifier 正常履职）
- **con_ID=6 状态演变**：negotiating → price→1.40 → 价格未达保持 → 发资金路径→detail_pending → "都发汇享"→confirmed → "好的"→confidence 0.95 ✓

---

## 五、总结

| 改进 | 一句话 |
|------|--------|
| 职责分离 | Extract 管要素，Judge 管状态，各司其职 |
| 状态追踪 | 从无状态到跨消息 ConversationState |
| ID 体系 | account 作 key → T1/T2 内部 ID，account 可为空 |
| 信号传递 | Extract 输出 linking_reason/status_signals/ambiguity 给 Judge |
| 矛盾检测 | Verifier 纯代码拦截图 ID 不一致、price 污染等 |
| 容错 | 超时自动重试 3 次 |
| 可审计 | 每步中间输出 + 字段级变更追溯 |
